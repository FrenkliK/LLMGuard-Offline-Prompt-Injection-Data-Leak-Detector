# Datasets

This folder holds sample datasets used to evaluate detectors. The default repo includes a tiny sample set for tests.

Use `download_datasets.sh` to fetch larger public datasets offline (no model APIs).
